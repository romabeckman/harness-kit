# Project Digest

## LAST UPDATED
2026-09-26

## ARCHITECTURE SUMMARY
Pattern: Ports and Adapters with a Chain-of-Responsibility orchestrator.
Stack: TypeScript, Node.js, Vitest 5.0.1, Docker.
Layers: domain/orchestration, application, adapters, composition. Inject ports through constructors/options; use factories/registries.
Flow: optional REFINEMENT -> BOOTSTRAP -> PLANNING -> DEVELOPMENT -> REVIEW -> TRANSITION -> MEMORY -> DEPLOY -> HALTED; CASCADE_BLOCKED handles dependents. Existing BACKLOG resumes at PLANNING+. Planning sets IN_PROGRESS; Review/Cascade terminate; Transition requires terminal status and queues the next feature.
Modes: thinking=LOW, deep_thinking=HIGH, undefined=AUTO; all use REFINEMENT. quick skips Review and emits 003/004; fast requires --refine. CLI defaults to Claude.
Context: Refinement uses pbb-design and SCOPE.md; retries/Review/Memory prefer REFINEMENT.md. Tasks require owners; TDD resumes at REVIEW; skipped Review has no synthetic scores.
Startup: Refinement asks up to 4 evidence-based setup questions. Bootstrap runs project-memory once for roots missing digest or graph, using the selected scope.
QA: Routed docs, JSON handoffs, scenario IDs, plan-only repair, runtime prerequisites, redacted evidence; adaptive analysis runs once. Reports derive from runtime results; verified targets expire after 30 days. Reuse CLI sessions; isolate browser contexts and wait for selectors/URLs. MCP supports legacy/stateless requests.
Server: One project per job; resume failed worktrees; completion requires every feature done. Handle branch deployment and shared secret screening.
REQUIRED: Use IFileStateManager and atomic replacement; propagate AbortSignal/prompt transport; resolve phase model/effort; validate DTOs; run RTK checks for code changes.
PROHIBITED: Concrete runners in orchestration decisions, HTTP transport in use cases, bypassing state adapters.
Docs: Group feature guides by domain folders under `docs/feature/`.

## TEST SUMMARY
Framework: Vitest 5.0.1; V8 coverage package configured.
Commands: `rtk npm run lint`, `rtk npm run build`, `rtk npm run typecheck`, `rtk npm run test`, `rtk npm run test:e2e`.
Coverage thresholds: None configured. Commands list available checks, not execution results.

## ROUTING
Use exact supplied paths directly; otherwise select a feature via `docs/.graph.json`. Read its `graph` and routed source for implementation, KNOWLEDGE and evidence for semantics, prose for design context.

## DOCUMENTATION INDEX
- `docs/adr/ARCHITECTURE.md` â€” architecture, layers, patterns, and integrations.
- `docs/adr/TESTS.md` â€” test tooling, commands, boundaries, and coverage limits.
- Required read `docs/.graph.json` for the complete document list, tags, and relations.
