# Project Digest

## LAST UPDATED
2026-09-18

## ARCHITECTURE SUMMARY
Pattern: Ports/adapters with chain-of-responsibility orchestrator.
Stack: TypeScript, Node.js, Vitest, Docker.
Flow: optional REFINEMENT -> BOOTSTRAP -> PLANNING -> DEVELOPMENT -> REVIEW -> TRANSITION -> MEMORY -> DEPLOY -> HALTED; CASCADE_BLOCKED handles dependency failures. REFINEMENT invokes `pbb-design`; Bootstrap and Planning consume its traceable business context while preserving provisional model assumptions. QA plans validate, execute, analyze, and report across HTTP, browser, mobile, accessibility, MCP, CLI, WebSocket, and security profiles.
Modes: `thinking` uses LOW with REFINEMENT; `deep_thinking` uses HIGH; undefined uses AUTO with REFINEMENT; `quick` skips Review; `fast` requires `--refine`.
Completion: `quick` emits 003/004; Bootstrap, Planning, Development retries, Review, and Memory prefer REFINEMENT.md over SCOPE.md, never both; Refinement consumes SCOPE.md; planning requires owned tasks; existing TDD output resumes at REVIEW; skipped review records no synthetic scores.
Layers: domain/orchestration, application, adapters, composition. QA CLI: `run`, `report`, `exploratory`, `auth`; analysis needs `--analysis` or analyzed resume. Reports support JSON, HTML, Markdown, and developer artifacts. Auth profiles redact evidence; authenticated browser runs omit login actions; `authProfile: "none"` scopes anonymous scenarios; authenticated WebSocket scenarios block.
QA uses routed docs, memory, and JSON handoffs; repair prompts escape NUL; renewal scopes actionable items; reset runs scope actionable results; verified targets expire after 30 days.
QA reuses runner sessions; browsers use isolated contexts and selector/URL waits; CLI executions start fresh.
DI: Inject ports through constructors or orchestrator options; use registries and factories at composition boundaries.
REQUIRED: Propagate AbortSignal/prompt transport, resolve phase model/effort, use atomic replacement, validate DTOs, and run RTK checks.
PROHIBITED: Bind orchestration decisions to concrete runners, place HTTP transport in use cases, or bypass state adapters.

## TEST SUMMARY
Framework: Vitest 5.0.0 with V8 coverage; E2E config `vitest.e2e.config.ts` uses 30-second test and hook timeouts.
Commands: `rtk npm run lint`, `rtk npm run build`, `rtk npm run typecheck`, `rtk npm run test`, `rtk npm run test:e2e`.
Coverage Thresholds: None configured; do not claim numeric gates.

## ROUTING
Use an exact supplied path directly. Otherwise use `docs/.graph.json` to select one feature, extract only its top `graph` block, then read routed source files. Read document prose only when the task requires design context.

## DOCUMENTATION INDEX
- `docs/adr/ARCHITECTURE.md` — architecture, layers, modules, patterns, and integrations.
- `docs/adr/TESTS.md` — test tooling, commands, boundaries, and troubleshooting.
- Required read `docs/.graph.json` for the complete document list, tags, and relations.
