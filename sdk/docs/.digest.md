# Project Digest

## LAST UPDATED
2026-09-20

## ARCHITECTURE SUMMARY
Pattern: Ports/adapters with chain-of-responsibility orchestrator.
Stack: TypeScript, Node.js, Vitest, Docker.
Flow: REFINEMENT? -> BOOTSTRAP -> PLANNING -> DEVELOPMENT -> REVIEW -> TRANSITION -> MEMORY -> DEPLOY -> HALTED; CASCADE_BLOCKED handles dependents. REFINEMENT is new-scope only; existing BACKLOG resumes at PLANNING+; TRANSITION queues next feature. Planning persists IN_PROGRESS; Review/Cascade set terminal status; Transition requires terminal status. Refinement uses `pbb-design`; Bootstrap/Planning use traceable context.
Modes: `thinking` uses LOW with REFINEMENT; `deep_thinking` uses HIGH; undefined uses AUTO with REFINEMENT; `quick` skips Review; `fast` requires `--refine`.
Completion: `quick` emits 003/004; Bootstrap/Planning/Development retries/Review/Memory prefer REFINEMENT.md; Refinement consumes SCOPE.md; tasks need owners; existing TDD resumes at REVIEW; skipped Review has no synthetic scores.
Layers: domain/orchestration, application, adapters, composition. QA actions: `run`, `report`, `exploratory`, `auth`; analysis needs `--analysis` or analyzed resume. Formats: JSON/HTML/Markdown/developer artifacts. Auth evidence is redacted; authenticated browsers omit login actions; `authProfile: "none"` means anonymous; authenticated WebSocket blocks.
QA uses routed docs, memory, and JSON handoffs; repair prompts escape NUL; renewal/reset target actionable results; verified targets expire after 30 days.
CLI run/diagnose/candidate review/QA runner paths prompt when `--agent` is omitted; Claude CLI is preselected; explicit `--agent` bypasses.
QA reuses runner sessions; browsers use isolated contexts and selector/URL waits; CLI executions start fresh.
DI: Inject ports via constructors/options; use registries and factories at composition boundaries.
REQUIRED: Propagate AbortSignal/prompt transport, resolve phase model/effort, use atomic replacement, validate DTOs, and run RTK checks.
PROHIBITED: Bind orchestration decisions to concrete runners, place HTTP transport in use cases, or bypass state adapters.

## TEST SUMMARY
Framework: Vitest 5.0.0 with V8 coverage; E2E config `vitest.e2e.config.ts` uses 30-second test and hook timeouts.
Commands: `rtk npm run lint`, `rtk npm run build`, `rtk npm run typecheck`, `rtk npm run test`, `rtk npm run test:e2e`.
Coverage Thresholds: None configured; do not claim numeric gates.

## ROUTING
Use an exact supplied path directly. Otherwise use `docs/.graph.json` to select one feature, extract only its top `graph` block, then read routed source files. Read document prose only when the task requires design context.

## DOCUMENTATION INDEX
- `docs/adr/ARCHITECTURE.md` — architecture, layers, modules, patterns, and integrations.
- `docs/adr/TESTS.md` — test tooling, commands, boundaries, and troubleshooting.
- Required read `docs/.graph.json` for the complete document list, tags, and relations.
