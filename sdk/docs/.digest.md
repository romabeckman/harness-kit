# Project Digest

## LAST UPDATED
2026-09-25

## ARCHITECTURE SUMMARY
Pattern: Ports and Adapters with a Chain-of-Responsibility phase orchestrator.
Stack: TypeScript, Node.js, Vitest 5.0.1, Docker.
Layers: orchestration and domain rules; application use cases; inbound and outbound adapters; composition and registries.
DI: Inject ports through constructors or orchestrator options; compose adapters with factories and registries.
REQUIRED: Persist state through `IFileStateManager`; use atomic replacement; propagate `AbortSignal`; validate server DTOs.
PROHIBITED: Bind orchestration decisions to concrete runners; put HTTP transport in use cases; bypass state adapters.
Docs: `docs/feature/` groups guides by agents, orchestration, QA, server, package, and terminal topics. Three existing feature paths stay stable for public root-guide links.

## TEST SUMMARY
Framework: Vitest 5.0.1; V8 coverage package configured.
Commands: `rtk npm run lint`, `rtk npm run build`, `rtk npm run typecheck`, `rtk npm run test`, `rtk npm run test:e2e`.
Coverage thresholds: None configured. Commands describe available checks; no checks ran for this documentation update.

## ROUTING
Use an exact supplied path directly. Otherwise select one feature from `docs/.graph.json` and read its top `graph` block. Read routed source files for implementation work; read `## KNOWLEDGE` and cited evidence for semantic questions. Read other prose only when design context is needed.

## DOCUMENTATION INDEX
- `docs/adr/ARCHITECTURE.md` — architecture, layers, patterns, and integrations.
- `docs/adr/TESTS.md` — test tooling, commands, boundaries, and coverage limits.
- Required read `docs/.graph.json` for the complete document list, tags, and relations.
