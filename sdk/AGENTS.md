# RULES

- Always start the caveman:caveman skill on ultra and read `docs/.digest.md` and `docs/.graph.json`
- Do NOT run `git push`, `git add`, `git commit`, `git restore` or `git checkout` commands (strictly forbidden)
- Use only English in your answers